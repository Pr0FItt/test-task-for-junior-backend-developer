# HISTORY

## 2026-04-28

### Что запросили

- Прочитать проект и добавить периодичность задач:
  - ежедневные
  - еженедельные
  - в определенные числа месяца
  - по четным/нечетным дням месяца

### Что было сделано

- Проанализирована структура проекта и текущая реализация CRUD задач.
- Добавлены поля периодичности в доменную модель задачи:
  - `recurrence_kind`
  - `recurrence_days`
- Добавлены типы периодичности:
  - `none`
  - `daily`
  - `weekly`
  - `monthly_dates`
  - `monthly_even_days`
  - `monthly_odd_days`
- Реализована валидация в usecase-слое:
  - для `weekly` дни только `1..7`, список обязателен
  - для `monthly_dates` дни только `1..31`, список обязателен
  - для `none`, `daily`, `monthly_even_days`, `monthly_odd_days` список дней должен быть пустым
  - дни нормализуются (удаление дублей и сортировка)
- Обновлены HTTP DTO и handlers для приема/выдачи новых полей.
- Обновлен PostgreSQL repository (insert/select/update/scan) для новых колонок.
- Обновлен OpenAPI (`/swagger/openapi.json`) и README.
- Выполнены `gofmt` и `go test ./...` (успешно).

### Миграции

- Изначально новые колонки были добавлены в `0001_create_tasks.up.sql`.
- По запросу добавлена отдельная миграция:
  - `migrations/0002_add_task_recurrence_columns.up.sql`
- `0001_create_tasks.up.sql` возвращен к базовой схеме без колонок периодичности.
- `docker-compose.yml` обновлен: добавлено подключение `0002` в `docker-entrypoint-initdb.d`.

### Как проверять

- Даны инструкции по проверке:
  - поднятие сервиса
  - проверка колонок в БД
  - happy-path запросы на создание/чтение/обновление
  - негативные кейсы валидации
  - запуск `go test ./...`
